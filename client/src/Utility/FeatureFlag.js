// Object consist of feature flag for the whole application.
// Technically this will be fetched from server or Headless CMS

export const FeatureFlag = {
  showNoOfLatestRecordForAllCustomer: 3, // Changing this flag will switch the UI and show records in table
};
